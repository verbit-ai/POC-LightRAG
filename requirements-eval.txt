# Extra packages required by run_eval.py on top of LightRAG's own deps.
# Install into the same virtualenv used for LightRAG.
ragas==0.4.3
datasets==4.8.5
langchain==1.2.15
langchain-core==1.3.2
langchain-openai==1.2.1
openai>=1.40
python-dotenv>=1.0
tiktoken>=0.7
